<Window x:Class="EditorDebug.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:EditorDebug"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        SizeToContent="WidthAndHeight"
        MinWidth="600"
        MinHeight="400"
        MaxWidth="2000"
        MaxHeight="1400"
        KeyDown="OnKeyDown"
        Title="MainWindow">
    <Window.Resources>
        <RoutedCommand x:Key="BrowseCommand"/>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource BrowseCommand}" Executed="OnBrowse"/>
    </Window.CommandBindings>
    <DockPanel Margin="10,10,10,10">
        <DockPanel DockPanel.Dock="Top">
            <Button DockPanel.Dock="Left" Command="{StaticResource BrowseCommand}">Browse...</Button>
            <TextBlock Text="{Binding CurrentFileName}" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </DockPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Focusable="False" Margin="0,10,0,0">
            <Grid x:Name="gridMain" Focusable="False"/>
        </ScrollViewer>
    </DockPanel>
</Window>
